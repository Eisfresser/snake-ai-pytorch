{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "agent --model pg",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "args": [
                "--model", "pg"
            ]
        }
    ]
}